# 第 14 章 综合评价方法

> 这份笔记是针对《Python数学建模算法与应用》第 14 章主题的原创学习讲解，重点放在原理、建模步骤和 Python 实现。

---

## 学习目标

掌握多指标评价中的标准化、赋权、排序和敏感性分析。

完成本章后，你应该能够：

1. 根据题目文字识别对应的数学模型类型。
2. 写出变量、约束、目标函数或评价指标。
3. 推导关键公式，并解释每个符号的现实含义。
4. 使用 Python 完成最小可运行求解。
5. 对结果做解释、检验和敏感性分析。

## 关键词

- 综合评价
- 熵权法
- TOPSIS
- AHP
- 灰色关联
- 主成分评价

## 先记住的核心公式

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

## 本章学习地图

```text
现实问题 -> 变量定义 -> 数学模型 -> 算法求解 -> Python 实现 -> 结果解释 -> 稳健性检查
```

## 考试复习总览

这一章复习时要按“识别题型、写出模型、说明算法、解释结果”的顺序准备。考试或课程报告通常不会只考一个 API，而是考你能不能把文字问题转成可计算模型。

### 高频考点

- 能识别 综合评价方法 适合处理什么类型的问题。
- 能写出变量、参数、目标函数、约束条件或评价指标。
- 能解释公式中每个符号的现实含义，而不是只抄公式。
- 能判断数据是否需要标准化、同向化、缺失值处理或时间顺序切分。
- 能选择 Python 中合适的函数或库，并说明为什么这样求解。
- 能根据输出结果写出一句清楚的建模结论。
- 能指出模型至少两个局限，并提出改进方向。

### 题型识别信号

- 题目出现“最优、最大、最小、成本、收益、资源、约束”，优先考虑优化模型。
- 题目出现“节点、道路、路径、网络、运输、连通”，优先考虑图论模型。
- 题目出现“多个指标、综合排名、评价对象、权重”，优先考虑综合评价模型。
- 题目出现“历史数据、趋势、未来、预测”，优先考虑预测模型。
- 题目出现“影响因素、解释变量、因变量、显著性”，优先考虑回归分析。
- 题目出现“分类、相似、分组、判别”，优先考虑聚类分析或判别分析。
- 题目出现“离散点、补全曲线、函数关系”，优先考虑插值与拟合。
- 题目出现“多重共线性、少样本、多指标解释响应”，优先考虑偏最小二乘。

### 答题固定结构

1. 先写“设……”：定义变量、样本、指标、节点或时间点。
2. 再写“建立模型如下”：列出目标、约束、公式或算法流程。
3. 然后写“求解方法”：说明使用的算法、库函数和关键参数。
4. 接着写“结果分析”：解释最优解、排名、预测值、分类结果或误差。
5. 最后写“模型评价”：说明优点、缺点、适用条件和改进方向。

### 公式速查

- $z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}$
- $e_j=-k\sum_i p_{ij}\ln p_{ij}$
- $C_i=\frac{D_i^-}{D_i^++D_i^-}$
- $CI=\frac{\lambda_{max}-n}{n-1}$

### 考前自测

- 不看笔记，能否在 3 分钟内写出本章适用场景？
- 不看代码，能否写出核心 Python 调用的输入和输出？
- 给一组小数据，能否手算或半手算一个结果用于校验程序？
- 能否解释为什么要标准化、同向化或加入约束？
- 能否说明模型失败时最先检查哪三件事？

## 章节目录

1. 评价指标体系
2. 指标同向化
3. 极差标准化
4. 熵权法
5. AHP 层次分析
6. TOPSIS
7. 灰色关联分析
8. 主成分综合评价
9. 排序稳定性
10. 敏感性分析

---

## 14.01 评价指标体系

### 问题意识

评价指标体系在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- 评价指标体系的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_1_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_1_standardize(X):
    X = section_14_1_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_1_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“评价指标体系”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“评价指标体系”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.02 指标同向化

### 问题意识

指标同向化在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- 指标同向化的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_2_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_2_standardize(X):
    X = section_14_2_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_2_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“指标同向化”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“指标同向化”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.03 极差标准化

### 问题意识

极差标准化在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- 极差标准化的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_3_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_3_standardize(X):
    X = section_14_3_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_3_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“极差标准化”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“极差标准化”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.04 熵权法

### 问题意识

熵权法在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- 熵权法的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_4_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_4_standardize(X):
    X = section_14_4_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_4_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“熵权法”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“熵权法”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.05 AHP 层次分析

### 问题意识

AHP 层次分析在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- AHP 层次分析的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_5_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_5_standardize(X):
    X = section_14_5_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_5_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“AHP 层次分析”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“AHP 层次分析”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.06 TOPSIS

### 问题意识

TOPSIS在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- TOPSIS的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_6_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_6_standardize(X):
    X = section_14_6_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_6_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“TOPSIS”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“TOPSIS”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.07 灰色关联分析

### 问题意识

灰色关联分析在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- 灰色关联分析的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_7_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_7_standardize(X):
    X = section_14_7_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_7_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“灰色关联分析”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“灰色关联分析”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.08 主成分综合评价

### 问题意识

主成分综合评价在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- 主成分综合评价的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_8_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_8_standardize(X):
    X = section_14_8_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_8_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“主成分综合评价”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“主成分综合评价”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.09 排序稳定性

### 问题意识

排序稳定性在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- 排序稳定性的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_9_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_9_standardize(X):
    X = section_14_9_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_9_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“排序稳定性”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“排序稳定性”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 14.10 敏感性分析

### 问题意识

敏感性分析在数学建模中通常不是为了展示算法，而是为了把一个现实问题压缩成“变量、约束、目标、数据、评价”五件事。
遇到任何建模题，先不要急着套包，先判断：哪些量可控，哪些量只能观测，哪些量是必须满足的约束，哪些量只是希望越大或越小越好。

### 建模步骤

1. 明确研究对象：写出样本、节点、时间点、指标或决策变量。
2. 明确输入数据：检查量纲、缺失值、异常点和数据来源可靠性。
3. 明确数学对象：判断使用矩阵、函数、图、概率模型、优化模型还是评价模型。
4. 建立目标函数或评价指标：说明为什么这个目标符合题意。
5. 加入约束条件：区分硬约束、软约束和经验假设。
6. 选择算法求解：优先使用稳定、可解释、可复现的算法。
7. 做结果解释：输出不只是数值，还要说明变量含义和现实建议。
8. 做敏感性分析：改变关键参数或数据扰动，观察结论是否稳定。

### 原理讲解

- 敏感性分析的核心是把现实语言转换成数学语言，转换质量往往比算法复杂度更重要。
- 如果变量定义不清楚，后面的公式即使正确也没有解释力。
- 如果约束条件漏掉，模型可能给出数学最优但现实不可执行的方案。
- 如果目标函数选错，算法会非常努力地优化一个错误方向。
- 建模时要保留单位和量纲意识，避免把不同尺度的数据直接相加。
- 计算结果要回到原问题解释，不能只给出矩阵、曲线或求解器状态。
- 对比赛或课程作业来说，清晰的假设、可复现代码和图表解释同样重要。
- 对工程问题来说，模型还需要考虑数据更新、运行时间和异常输入。

### 数学表达

本节常见表达可以统一写成：

$$
\text{data}\ \longrightarrow\ \text{model}\ \longrightarrow\ \text{solution}\ \longrightarrow\ \text{decision}
$$

$$
z_{ij}=\frac{x_{ij}-\min x_j}{\max x_j-\min x_j}
$$

$$
e_j=-k\sum_i p_{ij}\ln p_{ij}
$$

$$
C_i=\frac{D_i^-}{D_i^++D_i^-}
$$

$$
CI=\frac{\lambda_{max}-n}{n-1}
$$

符号解释：

- $x$ 通常表示待求变量或特征向量。
- $A$ 通常表示系数矩阵、约束矩阵或邻接矩阵。
- $b$ 通常表示资源上限、观测值或右端项。
- $f(x)$ 通常表示目标函数、误差函数或评价函数。
- $\theta$ 通常表示需要估计的模型参数。
- $\hat{y}$ 表示模型给出的预测值或拟合值。

### 推导线索

1. 从现实问题抽取变量，先写出变量表。
2. 根据变量关系写出等式、不等式或递推关系。
3. 把多个关系整理成矩阵形式，便于计算和检查维度。
4. 如果方程组无精确解，就转向最小误差原则。
5. 如果有多个目标，就用加权、分层或 Pareto 思想处理冲突。
6. 如果结果依赖主观权重，就必须做权重敏感性分析。
7. 如果模型含随机性，就固定随机种子并多次运行。
8. 如果算法可能陷入局部最优，就尝试多初值或全局搜索。

### Python 实现模板

```python
import numpy as np

def section_14_10_check_array(X):
    X = np.asarray(X, dtype=float)
    if np.isnan(X).any():
        raise ValueError('input contains NaN')
    if np.isinf(X).any():
        raise ValueError('input contains Inf')
    return X

def section_14_10_standardize(X):
    X = section_14_10_check_array(X)
    mu = X.mean(axis=0)
    sigma = X.std(axis=0) + 1e-12
    return (X - mu) / sigma, mu, sigma

def section_14_10_report(name, value):
    print(f'{name}: {value}')
```

### 结果解释

- 先解释最重要的变量或指标，不要先堆代码输出。
- 再解释为什么这个结果符合或不符合直觉。
- 接着说明约束是否全部满足。
- 然后报告误差、残差、最优值、排序分数或预测区间。
- 最后写出模型局限：数据量、假设、参数、算法、外推风险。

### 易错点

1. 没有统一量纲，导致大尺度指标支配结果。
2. 忘记检查可行性，直接相信求解器输出。
3. 把相关性解释成因果关系。
4. 用训练误差替代预测能力。
5. 没有说明主观权重来源。
6. 图模型中把有向边和无向边混用。
7. 优化模型中漏写非负约束或整数约束。
8. 预测模型中随机打乱了时间序列。

### 考试答题模板

如果题目考到“敏感性分析”，可以按下面这段结构组织答案：

1. 识别：本题属于 综合评价方法 中的“敏感性分析”类问题，目标是把实际问题转化为可计算模型。
2. 变量：设 $x_i$ 表示第 $i$ 个决策变量、评价值、状态量或待估参数。
3. 数据：将题目给出的表格整理为矩阵 $X$、向量 $y$、约束矩阵 $A$ 或图 $G$。
4. 模型：根据题意写出目标函数、约束条件、距离度量、递推公式或评价函数。
5. 求解：使用对应算法求解，并检查维度、可行性、收敛状态和误差。
6. 解释：把数学结果翻译回现实语言，例如最优方案、排名、预测趋势或分类结论。
7. 检验：用残差、误差、敏感性分析、交叉验证或可行性检查说明结果可靠性。

### 快速判分点

- 有没有清楚写出变量含义。
- 有没有写出核心公式，而不是只说“用 Python 求”。
- 有没有说明约束或指标方向。
- 有没有给出结果解释。
- 有没有指出模型局限。

## 本章完整 Python 示例

示例文件：[`14_evaluation.py`](../examples/14_evaluation.py)

```python
import numpy as np

def minmax_positive(X):
    X = np.asarray(X, dtype=float)
    return (X - X.min(axis=0)) / (X.max(axis=0) - X.min(axis=0) + 1e-12)

def entropy_weight(X):
    Z = minmax_positive(X) + 1e-12
    P = Z / Z.sum(axis=0, keepdims=True)
    n = X.shape[0]
    E = -(P * np.log(P)).sum(axis=0) / np.log(n)
    D = 1 - E
    return D / D.sum()

def topsis(X, weights=None):
    Z = minmax_positive(X)
    norm = Z / (np.sqrt((Z ** 2).sum(axis=0, keepdims=True)) + 1e-12)
    if weights is None:
        weights = entropy_weight(X)
    V = norm * weights
    best = V.max(axis=0)
    worst = V.min(axis=0)
    d_pos = np.sqrt(((V - best) ** 2).sum(axis=1))
    d_neg = np.sqrt(((V - worst) ** 2).sum(axis=1))
    score = d_neg / (d_pos + d_neg + 1e-12)
    return score, np.argsort(-score)

if __name__ == "__main__":
    X = np.array([[80, 70, 90], [90, 65, 85], [75, 95, 80], [88, 82, 78]], dtype=float)
    print("weights:", entropy_weight(X))
    print("topsis:", topsis(X))
```

## 典型题型与解题套路

### 概念辨析题

题目特征：说明 综合评价方法 适合解决什么问题，并给出不适用的情况。

答题步骤：

1. 先写适用场景。
2. 再写模型输入和输出。
3. 然后写关键公式。
4. 最后写局限：数据要求、假设条件、计算限制。

### 建模推导题

题目特征：给出一段文字背景，要求建立数学模型。

答题步骤：

1. 划出题目中的可控量、已知量和评价目标。
2. 定义变量和参数。
3. 写出目标函数或评价函数。
4. 逐条翻译约束条件。
5. 说明求解方法和结果解释方式。

### 代码实现题

题目特征：给出数据表，要求用 Python 求解并输出结果。

答题步骤：

1. 读入数据并检查缺失值。
2. 根据模型需要做标准化、同向化或矩阵转换。
3. 调用 NumPy、SciPy、scikit-learn、statsmodels 或 NetworkX。
4. 输出核心结果，不只打印对象。
5. 用一个误差或可行性指标检查结果。

### 结果分析题

题目特征：给出模型输出，要求解释含义并评价模型。

答题步骤：

1. 先解释数值含义。
2. 再联系原问题给出结论。
3. 然后检查约束、误差或排序稳定性。
4. 最后指出局限和改进方向。

### 小型模拟题

1. 已知一组与“评价指标体系”有关的数据，请建立模型、写出核心公式、给出 Python 求解思路，并说明如何检验结果。
2. 已知一组与“指标同向化”有关的数据，请建立模型、写出核心公式、给出 Python 求解思路，并说明如何检验结果。
3. 已知一组与“极差标准化”有关的数据，请建立模型、写出核心公式、给出 Python 求解思路，并说明如何检验结果。
4. 已知一组与“熵权法”有关的数据，请建立模型、写出核心公式、给出 Python 求解思路，并说明如何检验结果。
5. 已知一组与“AHP 层次分析”有关的数据，请建立模型、写出核心公式、给出 Python 求解思路，并说明如何检验结果。
6. 已知一组与“TOPSIS”有关的数据，请建立模型、写出核心公式、给出 Python 求解思路，并说明如何检验结果。

## 建模报告写作模板

1. 问题重述：用自己的话写清楚研究对象和任务。
2. 模型假设：列出简洁、必要、可辩护的假设。
3. 符号说明：变量、参数、数据来源、单位。
4. 模型建立：给出目标函数、约束、评价指标或递推关系。
5. 模型求解：说明算法、软件包、关键参数和随机种子。
6. 结果展示：用表格、图像和文字解释结果。
7. 检验分析：残差、误差、可行性、敏感性、稳定性。
8. 模型评价：优点、缺点、可改进方向。

## 复习题

1. 围绕“评价指标体系”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
2. 围绕“指标同向化”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
3. 围绕“极差标准化”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
4. 围绕“熵权法”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
5. 围绕“AHP 层次分析”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
6. 围绕“TOPSIS”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
7. 围绕“灰色关联分析”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
8. 围绕“主成分综合评价”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
9. 围绕“排序稳定性”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
10. 围绕“敏感性分析”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
11. 围绕“评价指标体系”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
12. 围绕“指标同向化”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
13. 围绕“极差标准化”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
14. 围绕“熵权法”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
15. 围绕“AHP 层次分析”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
16. 围绕“TOPSIS”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
17. 围绕“灰色关联分析”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
18. 围绕“主成分综合评价”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
19. 围绕“排序稳定性”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。
20. 围绕“敏感性分析”设计一个小型数学建模问题，写出变量、公式、求解方法和 Python 检验方式。

## 考前最后检查表

- 我能说出 综合评价方法 的适用题型和不适用题型。
- 我能默写本章核心公式并解释符号。
- 我能把题目文字翻译成变量、参数和数据矩阵。
- 我能说明为什么需要标准化、同向化、权重或约束。
- 我能写出至少一个 Python 最小求解模板。
- 我能解释求解器输出中的最优值、参数、误差或标签。
- 我能写出两条模型局限。
- 我能做一个简单的敏感性分析。
- 我能区分训练误差、预测误差、拟合误差和评价分数。
- 我能在报告中把公式、代码、表格和结论连起来。
