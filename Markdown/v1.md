### 一、时间复杂度
1. **计算最内层的时间频度**
    ```
    for (int i = 1; i <= n; i++) // n+1
    {
        for (int j = 1; j <= n; j++) // n*(n+1)
        {
            c[i][j] = 0; // n*n
            for (int k = 1; k <= n; k++) // n*n*(n+1)
            {
                c[i][j] = c[i][j] + a[i][k] * b[k][j]; // n*n*n
            }
        }
    }
    ```
2. **只保留最里面的最高次项，系数为一**
   > n^3
3. **这个就是这段代码的时间复杂度**
    1. 